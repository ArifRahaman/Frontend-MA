# Copy to .env.local for local dev (optional — dev uses the Vite proxy anyway).
# On Vercel, set this in Project Settings -> Environment Variables.
# It must point to your deployed backend (no trailing slash):
VITE_API_BASE=https://your-iffat-backend.onrender.com
